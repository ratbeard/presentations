Slides for my presentations.

Files are grouped by topic.  To make 1 file per slide (presentation mode) run:

  rake build
  
To open all the slides in order run:

  mate slides
  rake open
    
now move through slides with:

  cmd+opt+arrows
